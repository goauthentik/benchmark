---
common_packages:
  - htop
  - nano

common_docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

# Debian package architecture names differ from the kernel's.
common_docker_apt_architectures:
  x86_64: amd64
  aarch64: arm64
  armv7l: armhf
